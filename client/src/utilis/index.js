import {supriseMePrompt} from '../constants '


export function getRandomPrompt(prompt){
    const randomIndex =
    Math.floor(Math.random()* supriseMePrompt.length)
    const randomPrompt = supriseMePrompt[]
 }
